function solution(number){
  if (number < 0) return 0;

  return Array.from({ length: number }, (_, i) => i) // generate [0..n-1]
    .filter(num => num % 3 === 0 || num % 5 === 0) // keep multiples of 3 or 5
    .reduce((sum, num) => sum + num, 0); // add them up
}


/**
 * 
 * 
 */